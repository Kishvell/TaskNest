const contributorDiv = document.querySelector("#contributor");
const contributorAdd = document.querySelector("#addContributor");
const taskList = document.querySelector("#taskList");
const addColumn = document.querySelector("#addColumn");
const addContributorButton = document.querySelector("#addContributorButton");
const addColumnButton = document.querySelector("#addColumnButton");

contributorAdd.addEventListener("click", () => {
    //show modal for adding contributors
});

addColumn.addEventListener("click", () => {
    //show modal for adding columns
})

addContributorButton.addEventListener("click", () => {
    //adds user for the project
})

addColumnButton.addEventListener("click", () => {
    //adds column in the project
})